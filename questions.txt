1. Find the number of ways in which array can be devided into 3 parts such that sum element in each part are equal.

2. Devide array of n size into m parts such that maximum of each parts should be minimum.

3. Check if the given number is additive(A + B = B) or not.

4. Find top <N> most frequent words from ;arge document based on below condition:
   a. ignore the stop words(a, the, is, am ....)
   b. if count is same return in alphabetical order

5. using binary random number generator, generate random number from 1 to 9.
